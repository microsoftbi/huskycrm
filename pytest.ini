[pytest]
asyncio_mode = auto
testpaths = TEST/Unittest
pythonpath = backend
python_files = test_*.py
python_classes = Test*
python_functions = test_*
filterwarnings =
    ignore::DeprecationWarning
markers =
    uat: UAT tests (require frontend + backend running)
